interface:
  display_name: "Codex Setup Check"
  short_description: "Confirm the repo-local setup, then send the user to the executable setup doctor."
  default_prompt: "Check whether this repository has the expected repo-local Codex setup. Confirm the required AGENTS.md, skills, metadata, and optional config examples, then direct the user to run the appropriate codex-doctor script and interpret its output."
